# دليل ميزة الرسائل الصوتية 🎤

تمت إضافة ميزة تسجيل وإرسال الرسائل الصوتية إلى تطبيق Groupify. هذا الدليل يشرح كيفية استخدام هذه الميزة والخطوات المطلوبة لتشغيلها.

## الميزات الجديدة 🎯

✅ **تسجيل الرسائل الصوتية** - سجل رسائل صوتية مباشرة من واجهة المحادثة  
✅ **رفع تلقائي** - الملفات الصوتية يتم رفعها تلقائياً على Firebase Storage  
✅ **تشغيل الصوت** - اسمع الرسائل الصوتية مباشرة في المحادثة  
✅ **عرض المدة** - يظهر الوقت الحالي والمدة الكلية للملف الصوتي  
✅ **واجهة سهلة الاستخدام** - ازرار واضحة للتسجيل والتشغيل والإيقاف  

## الملفات المضافة/المعدلة 📁

### ملفات جديدة:
- `lib/Services/audioService.dart` - خدمة إدارة التسجيل والرفع
- `lib/Widgets/audioMessageWidget.dart` - عنصر عرض الرسائل الصوتية

### ملفات معدلة:
- `pubspec.yaml` - إضافة المكتبات المطلوبة
- `lib/Screens/chatScreen.dart` - إضافة واجهة المستخدم والتحكم بالتسجيل
- `android/app/src/main/AndroidManifest.xml` - إضافة صلاحيات Android
- `ios/Runner/Info.plist` - إضافة وصف الميكروفون لـ iOS

## المكتبات المضافة 📚

تمت إضافة المكتبات التالية إلى `pubspec.yaml`:

```yaml
record: ^5.1.0                  # مكتبة التسجيل الصوتي
permission_handler: ^11.4.4     # إدارة الصلاحيات
path_provider: ^2.1.2           # للوصول إلى مسارات الملفات
just_audio: ^0.9.38             # تشغيل الملفات الصوتية
```

## خطوات الإعداد 🚀

### 1. تثبيت المكتبات
افتح Terminal في مجلد المشروع وشغل:
```bash
flutter pub get
```

### 2. صلاحيات Android
تمت إضافة الصلاحيات التالية تلقائياً في `AndroidManifest.xml`:
- `RECORD_AUDIO` - للتسجيل الصوتي
- `INTERNET` - لرفع الملفات
- `WRITE_EXTERNAL_STORAGE` - لحفظ الملفات
- `READ_EXTERNAL_STORAGE` - لقراءة الملفات

### 3. صلاحيات iOS
تمت إضافة وصف الميكروفون تلقائياً في `Info.plist`:
- `NSMicrophoneUsageDescription` - شرح لاستخدام الميكروفون

## كيفية الاستخدام 💬

### إرسال رسالة نصية:
1. اكتب الرسالة في حقل النص
2. اضغط على زر الإرسال (الورقة الزرقاء)

### إرسال رسالة صوتية:
1. اضغط على زر الميكروفون (الأخضر) في أسفل الشاشة
2. سيبدأ التسجيل - ستظهر مؤشر تحمر "جاري التسجيل"
3. تحدث إلى الميكروفون
4. اضغط على زر التوقف (الأحمر) عندما تنتهي
5. الملف سيتم رفعه تلقائياً وإرساله في المحادثة

### تشغيل رسالة صوتية:
1. ابحث عن الرسالة الصوتية في المحادثة (ستظهر بتصميم خاص)
2. اضغط على زر التشغيل (▶️)
3. الشريط سيظهر تقدم التشغيل
4. اضغط على زر الإيقاف (⏸️) لإيقاف التشغيل

## هيكل البيانات في Firestore 💾

الرسائل الصوتية يتم تخزينها بالهيكل التالي:

```json
{
  "type": "audio",
  "audioUrl": "https://...",
  "duration": 15,
  "sender": "user@example.com",
  "name": "اسم المستخدم",
  "photoUrl": "https://...",
  "userId": "uid",
  "timestamp": "firebase_timestamp",
  "createdAt": 1234567890,
  "isAnonymous": false
}
```

### حقول الرسالة الصوتية:
- `type`: "audio" - نوع الرسالة
- `audioUrl`: رابط الملف الصوتي على Firebase Storage
- `duration`: مدة التسجيل بالثواني
- `sender`: بريد المرسل
- `name`: اسم المرسل
- `photoUrl`: صورة المرسل
- `userId`: معرف المستخدم الفريد
- `timestamp`: وقت الإرسال (Server Timestamp)
- `createdAt`: وقت الإنشاء بـ milliseconds
- `isAnonymous`: هل المرسل مستخدم مجهول

## عمارة الكود 🏗️

### AudioService (في Services/audioService.dart)
مسؤول عن:
- بدء وإيقاف التسجيل الصوتي
- رفع الملفات على Firebase Storage
- حساب مدة التسجيل
- حذف الملفات المؤقتة

**الدوال الرئيسية:**
- `startRecording()` - بدء التسجيل
- `stopRecording()` - إيقاف التسجيل
- `uploadAudioToFirebase()` - رفع الملف
- `getAudioDuration()` - حساب المدة
- `deleteRecordingFile()` - حذف الملف المؤقت
- `dispose()` - تنظيف الموارد

### AudioMessageWidget (في Widgets/audioMessageWidget.dart)
عنصر واجهة لعرض الرسائل الصوتية:
- زر التشغيل/الإيقاف
- شريط التقدم
- عرض المدة الحالية والكلية
- تصميم يتطابق مع الرسائل النصية

### ChatScreen التعديلات
في `Screens/chatScreen.dart`:
- إضافة متغيرات التسجيل (`_isRecording`, `_recordingDuration`)
- دوال التسجيل (`_startAudioRecording`, `_stopAudioRecording`)
- تعديل عرض الرسائل لدعم الرسائل الصوتية
- تعديل الواجهة لإضافة زر التسجيل

## معالجة الأخطاء 🛡️

التطبيق يتعامل مع الأخطاء التالية:
- ❌ عدم إعطاء صلاحيات الميكروفون
- ❌ فشل بدء التسجيل
- ❌ فشل رفع الملف
- ❌ فشل تشغيل الملف الصوتي
- ❌ عدم وجود مستخدم مسجل دخول

كل خطأ يظهر برسالة واضحة للمستخدم.

## الخصوصية والأمان 🔐

- الملفات الصوتية يتم تخزينها في Firebase Storage
- كل ملف صوتي يحصل على اسم فريد يتضمن معرف المستخدم والوقت
- يمكنك إعداد قواعد Firestore للتحكم في الوصول للملفات الصوتية
- الملفات المؤقتة يتم حذفها بعد الإرسال

## حل المشاكل 🔧

### المشكلة: "Target of URI doesn't exist"
**الحل:** شغل `flutter pub get` لتثبيت المكتبات المطلوبة

### المشكلة: لا يوجد صوت عند التشغيل
**الحل:** تحقق من:
- اتصال الإنترنت
- صلاحيات الميكروفون مفعلة
- القيمة الصحيحة لـ audioUrl في Firestore

### المشكلة: رسالة "تم رفض صلاحيات الميكروفون"
**الحل:** 
- روح للإعدادات → Applications → Groupify
- فعّل صلاحية الميكروفون

## الخطوات التالية 🚀

اختياري - يمكنك إضافة:
- ✨ تأثيرات صوتية (نغمة للإرسال)
- ✨ إمكانية حذف الرسائل الصوتية
- ✨ إمكانية إعادة التسجيل قبل الإرسال
- ✨ عرض موجات الصوت أثناء التسجيل
- ✨ تحديد جودة الصوت

## اختبار الميزة ✅

لاختبار الميزة:
1. قم بتشغيل التطبيق على جهازك أو محاكي
2. سجل دخول كمستخدم
3. اذهب للمحادثة الجماعية
4. اضغط على زر الميكروفون وسجل رسالة صوتية
5. توقف عن التسجيل واسمح برفع الملف
6. تحقق من ظهور الرسالة الصوتية في المحادثة
7. اضغط على التشغيل واستمع للرسالة

## استكشاف الأخطاء 🐛

إذا واجهت مشاكل:
1. افتح Android Logcat أو iOS Console
2. ابحث عن رسائل الخطأ التي تبدأ بـ "❌" أو "🔄"
3. تحقق من firebase.json والصلاحيات
4. تأكد من أن Firebase Storage مفعّل وله قواعد صحيحة

## الدعم 📞

للمزيد من المعلومات:
- قراءة التعليقات في الكود
- مراجعة وثائق المكتبات المستخدمة:
  - https://pub.dev/packages/record
  - https://pub.dev/packages/just_audio
  - https://pub.dev/packages/permission_handler

---

**ملاحظة:** جميع التعليقات في الكود بالعربية لسهولة الفهم! 🎉
